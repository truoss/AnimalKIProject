using UnityEngine;

public class AnimalBrain : ScriptableObject
{
    /*
    static AnimalBrain instance;
    public static AnimalBrain I
    {
        get
        {
            if (instance == null)
            {
                instance = Resources.Load<AnimalBrain>("AnimalBrain");

                if (instance == null)
                    Debug.LogError("Could not load AnimalBrain, make sure it is exported with the built executeable!");

                //DontDestroyOnLoad(instance);
            }

            return instance;
        }
    }
    */
}
